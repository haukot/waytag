= simple_form_for @city_street, url: url, html: {class: "form-horizontal"} do |f|
  = f.error_notification

  = f.input :name
  = f.association :city

  .form-group
    .col-sm-10.col-sm-offset-2
      = f.button :submit
