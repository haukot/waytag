= simple_form_for @user, :url => url, :html => {:class => "form-horizontal"} do |f|
  = f.error_notification
  = f.input :login
  = f.input :password
  .control-group
    .controls
      = f.button :submit, :class => 'btn btn-primary'
